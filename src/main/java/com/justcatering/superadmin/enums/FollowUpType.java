package com.justcatering.superadmin.enums;

/**
 * Type of client follow-up activity.
 */
public enum FollowUpType {

    /** Phone call. */
    CALL,

    /** In-person or virtual meeting. */
    MEETING,

    /** Email correspondence. */
    EMAIL,

    /** On-site visit. */
    VISIT,

    /** Other follow-up activity. */
    OTHER
}
